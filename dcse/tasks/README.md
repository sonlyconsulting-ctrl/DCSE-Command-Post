# Legacy Task Location Pointer

Canonical DCSE task packets live only under `tasks/queue/`.

Use:

```bash
python -m dcse tasks
```

Then open the referenced `tasks/queue/<id>.json`. No task packet under `dcse/tasks/` is authoritative.
