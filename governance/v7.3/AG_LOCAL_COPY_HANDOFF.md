# Anti-Gravity (AG) Local Copy Handoff | v7.3
**Task ID:** DCSE-V73-LOCAL-CONSOLIDATION-20260919-01
**Assignment:** Authorized junior DBA / local file-copy executor, not constitutional architect, promotion authority or final validator. General DCSE lane only. No credentials and no PS-origin source content in model-visible output.
**Destination requested by DCS:** `C:\DS All Things\DCSE_Command_Center\v7.3\governance`.
**GitHub source:** `sonlyconsulting-ctrl/DCSE-Command-Post` **reviewed/approved v7.3 source** at an exact commit from `main` after PR review and the applicable DCS decision. Until then, a review-branch preview is explicitly `CANDIDATE_ONLY`; do not label a candidate folder operative.

## AG sequence (only upon DCS-authorized handoff)
1. Authenticate to GitHub with existing scoped runtime credentials; verify repo, actual branch, expected v7.3 commit and `governance/v7.3/V7_3_DEVELOPMENT_PACKAGE_MANIFEST.json`. Record commit, root inventory and manifest identity. Do not run from local `feature/escd-runtime-content-002`, locally untracked `v7.2` assets, or an unknown working tree.
2. Verify the Windows target volume and path. Inspect any existing `v7.3` folder and user modifications; if occupied or conflicting, pause only the overlapping write and report. Never reset/clean/rename/delete `v7.2`, the historical v7.1 folder or unrelated local roots. Confirm source and target lane/classification eligibility; PS D13/D14 originals are excluded.
3. Prepare a collision-safe temporary staging subfolder under the authorized destination's parent, with one writer and a pre-copy destination-state inventory. Copy only `governance/v7.3/**` from the exact approved commit, preserving relative hierarchy, file content, source commit and intended casing. Never copy `governance/v7.2/**` into the operative v7.3 tree or blindly import unrelated root files.
4. Compare tree paths, byte counts and SHA-256 for each source and staged destination; preserve a machine manifest and list any mismatch, untracked addition, absent dependency or unsafe file. The `reference/v7_2_mainline/**` folder is historical context; the `doctrines/**` subtree is a v7.3 development candidate until separately promoted. No blanket chmod/access broadening.
5. Promote the staged local **working representation** to the target only if exact destination has no conflicting user files and the copy/adoption action is authorized. Do not treat local placement as canonical GitHub governance or as D05 doctrine promotion. If staging validation fails, leave the existing source/local target untouched and report PARTIAL with recovery path.
6. Record source repo/ref/commit, local full path, copied count, byte/hash results, protected exclusions, opened PR status, existing-v7.2 untouched proof, validation, actor, timestamps and receipt to the authorized Tribunal/intake route. If unable to deliver receipt, label `RECEIPT_PENDING`; no invented sync or runtime consumption.
7. Do not touch Supabase, GitHub main, PR merge, protected local folders, media binaries, deployments, or secrets. No post-copy automatic cleanup. Escalate unresolved classification, destination conflict or promotion questions to Command Post/DCS.

**Exit:** Local representation copied + byte verified + reviewed. Production runtime synchronization / D22 registry updates require a separate authorized task.
